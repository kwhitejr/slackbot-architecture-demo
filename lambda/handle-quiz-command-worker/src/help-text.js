const helpText = `Quiz command initiates a quiz.

Usage
@param name {String} any
@param type {String} one of [modal, message, home]

/foobar name=Zeus&type=modal

References
- name: it's your name
- type: https://api.slack.com/surfaces
`;

module.exports = {
  helpText
};
